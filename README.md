# AMERIND Wildfire Risk Platform

Two tools hosted on GitHub Pages:

- `/` — Single address wildfire risk assessment
- `/monitor.html` — Portfolio monitoring dashboard

## Setup
1. Open `monitor.html` in a text editor
2. Replace `YOUR_SUPABASE_URL` and `YOUR_SUPABASE_ANON_KEY` with your values
3. Push to GitHub and enable GitHub Pages
